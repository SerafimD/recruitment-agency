<%= form_for(vacancy) do |f| %>
  <% if vacancy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vacancy.errors.count, "error") %> prohibited this vacancy from being saved:</h2>

      <ul>
      <% vacancy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :addition_date %>
    <%= f.datetime_select :addition_date %>
  </div>

  <div class="field">
    <%= f.label :duration %>
    <%= f.datetime_select :duration %>
  </div>

  <div class="field">
    <%= f.label :salary %>
    <%= f.number_field :salary %>
  </div>

  <div class="field">
    <%= f.label :contact %>
    <%= f.text_area :contact %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
